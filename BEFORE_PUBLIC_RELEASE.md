# Before public release

- Confirm the final paper URL and add it to `README.md` and `CITATION.cff`.
- Confirm that all coauthors approve the public checkpoint and Apache-2.0 release.
- Run the checkpoint evaluation on a CUDA machine and record the deterministic public metric.
- Resolve the manuscript's 100-epoch statement versus the attached checkpoint's 300-epoch
  pretraining history.
- Run the selected from-scratch configuration at least once from a clean environment.
- Tag `v0.1.0` and optionally archive the release with Zenodo.
